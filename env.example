# PostgreSQL Database (Neon)
DATABASE_URL=postgresql://user:password@host:5432/database?sslmode=require

# Maid Central API
# MAID_CENTRAL_API_BASE_URL=https://api.maidcentral.com (Hardcoded in code now)

# GoHighLevel Private Token (configured via UI, not needed here)
# Private tokens are stored securely in Vercel KV after setup

# App Base URL (for webhooks and OAuth redirects)
APP_BASE_URL=http://localhost:3000

